#ifndef __RAND_H__
#define __RAND_H__

#include <types.h>

/**
 * @brief Returns a pseudo-random integer.
 *        Generator is seeded with a hardware noise source
 */
i32 rand(void);

#endif /* __RAND_H__ */
